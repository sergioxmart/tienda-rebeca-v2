// Placeholder. En T1+ se reemplazan con tests reales.

import { test } from 'node:test';
import { strict as assert } from 'node:assert';

test('core/test existe y node:test corre', () => {
  assert.equal(1 + 1, 2);
});
